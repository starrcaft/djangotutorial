# djangotutorial

이 튜토리얼은 django 생성 - model - migrate - react 연동으로 이루어졌다.
위 순서로 commit을 했다.

간단하게 설명을 하자면 django의 migrate까지 와 react 연동을 별개이며
react 연동은 rest framework를 이용했다

필요 pip
pip install django~=version
pip install djangoframework


